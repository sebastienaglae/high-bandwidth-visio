{
  "name": "@visio/web",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@visio/shared": "*",
    "mediasoup-client": "^3.7.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^8.2.2"
  }
}
