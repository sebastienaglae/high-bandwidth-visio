{
  "name": "@visio/desktop",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "android:init": "npm run tauri -- android init --ci",
    "android:dev": "npm run tauri -- android dev",
    "android:build": "npm run tauri -- android build",
    "ios:init": "npm run tauri -- ios init --ci",
    "ios:dev": "npm run tauri -- ios dev",
    "ios:build": "npm run tauri -- ios build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
